import React from 'react';
import { Routine } from '@/types';

interface RoutineCardProps {
  routine: Routine;
}

const RoutineCard: React.FC<RoutineCardProps> = ({ routine }) => {
  return (
    <div className="bg-gray-100 rounded-lg p-4">
      <h3 className="font-semibold mb-2">{routine.title}</h3>
      <div className="flex flex-col gap-1 mb-3">
        <div className="flex justify-between">
          <span className="text-sm">Series:</span>
          <span className="text-sm">{routine.series}</span>
        </div>
        <div className="flex justify-between">
          <span className="text-sm">Repeticiones:</span>
          <span className="text-sm">{routine.repetitions}</span>
        </div>
      </div>
      <button className="w-full py-1 px-4 bg-blue-400 text-white rounded-lg text-sm">
        Ver rutina
      </button>
    </div>
  );
};

export default RoutineCard;